@model OdeToFood.ViewModel.HomeIndexViewModel
@{
    ViewBag.Title = "Home Page";

}


<h1>ALL Restaurants</h1>

@foreach (var restaurant in Model.Resturants)
{
    @Html.Partial("_Summary", restaurant)
}

@*<a href="/home/details/@restaurant.Id">Details</a> THIS IS INSIDE THE TABLE FOR THE ROOUTE
    @Html.ActionLink("Go", "Details", new { id = restaurant.Id })*@
@*<table>
        @foreach (var restaurant in Model.Resturants)
        {
            <tr>
                <td>@restaurant.Id</td>
                <td>@restaurant.Name</td>

                <td>

                    <a asp-action="Details" asp-route-id="@restaurant.Id">More</a>
                    <a asp-page="/Restaurants/Edit" asp-route-id="@restaurant.Id">Edit</a>
                </td>
            </tr>
        }
    </table>*@
<div>
    <a class="btn btn-primary" asp-action="Create">Add Restaurant</a>
</div>

